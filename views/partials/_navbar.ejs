<link rel="stylesheet" href="/stylesheets/partials.css" />

<nav>
  <% if(user) { %>
  <div class="navbar">
    <h1>Bigfoot Forum</h1>
      <div class="nav-links">
        <a href="/users/<%= user._id %>/sightings">Your Sightings</a>
        <a href="/auth/sign-out">Sign Out</a>
      </div>
  </div>
  <% } else { %>
  <div class="navbar"> 
    <h1>Bigfoot Forum</h1>
      <a href="/auth/sign-in">Sign In</a>
      <a href="/auth/sign-up">Sign Up</a>
  </div>
  <% } %>
</nav>